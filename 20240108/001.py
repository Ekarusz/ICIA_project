# 타입(원시 타입) -> import : 타입의 기능을 확장(남이 만든 것을 가져다 쓸 수 있어서 더 많은 타입을 사용 할 수 있음)
# 값을 하나 저장 : int, float, str, bool
            # 숫자 -> int(정수), float(실수)
            # 문자열 -> str
            # 참거짓 -> bool
# 값을 여러개 저장(sequence) : list, tuple, dictionary, set
            # list -> [1,2,3,]
            # tuple -> (1,2,3)  읽기 전용
            # set -> {1,2,3}
            # dictionary
        # 순서대로 읽고 쓴다 : list, tuple
        # [175,160,180]  (175,160,180)
        # 계산으로 위치 파악 : set {175,160,180}
        # 키와 값의 쌍(pair) : dictionary
        # {'홍길동':175, '전우치':160, '임꺽정':180}


['spring','1234']   #list
{
    'id':'spring',
    'pw':'1234'
}   #dictionary

